{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn gstongo.wsgi:application --bind 0.0.0.0:$PORT"
  },
  "name": "gstongo-backend"
}
