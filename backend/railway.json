{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn gstongo.wsgi:application --bind 0.0.0.0:8000",
    "healthcheckPath": "/api/v1/auth/profile/"
  },
  "name": "gstongo-backend"
}
